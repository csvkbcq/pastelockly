if the secret key is given also the decrypt key after applying showing errors that is not working check with this ....

steps to run:
1. delete this comment file
2. install the requirements using the command  'pip install -r requirements.txt'
3.run command 'python app.py'
